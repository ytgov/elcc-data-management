/**
 * Global directive stubs for custom directives used in the app.
 * These can be spread into the global.directives option when mounting components.
 *
 * Usage:
 *   import { directiveStubs } from "@/tests/support"
 *
 *   mount(Component, {
 *     global: {
 *       directives: directiveStubs,
 *     },
 *   })
 */
export const directiveStubs = Object.freeze({
  // visible: {},
})

export default directiveStubs
